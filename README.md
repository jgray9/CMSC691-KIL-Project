Requires libraries:

`https://pytorch.org/get-started/locally/`

`pip install transformers datasets accelerate evaluate hf_xet scikit-learn spacy nltk pywebview`

`python -m spacy download en_core_web_sm`